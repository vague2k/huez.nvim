return {
  colorscheme = require("huez-manager.api.colorscheme"),
  live = require("huez-manager.api.live"),
  ensured = require("huez-manager.api.ensured"),
  favorites = require("huez-manager.api.favorites"),
}
