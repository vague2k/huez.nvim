return {
  themes = require("huez.pickers.themes").render,
  live = require("huez.pickers.live").render,
  ensured = require("huez.pickers.ensured").render,
  favorites = require("huez.pickers.favorites").render,
}
